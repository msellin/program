# Cloudflare Pages project config. Deployed via `wrangler pages deploy`.
name = "program-v2"
compatibility_date = "2026-01-15"
pages_build_output_dir = "./out"

# KV binding removed 2026-08-18 · Phase 2F of the KV → Postgres migration.
# User state now lives in Supabase Postgres; see src/lib/persistence/
# postgres-adapter.ts. The `STORE` KV namespace itself is kept in Cloudflare
# for a rollback window and can be deleted once we're confident (~2 weeks
# after this ships).
