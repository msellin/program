# Session audit backlog — what remains

> **Triaged 2026-09-08.** Every `- [ ]` in the original was checked against the
> codebase, not against its own description. Result: **10 already done and
> never ticked · 5 obsolete or superseded · 4 genuinely remaining.**
>
> The full original is preserved at
> `dev/archive/triaged-2026-09-08/active__session-audit-2026-08-17__backlog.md` — nothing was deleted, and the
> DONE verdicts are recoverable from there. What follows is only what survived,
> so the generated index in `dev/active/OPEN-TASKS.md` counts reality instead of
> counting boxes nobody walked back to tick.

## Remaining

- [ ] **M5 — modal stacking.** The `z-[60]` patch in `OnboardingRunner.tsx:88` is
      still the only fix. No modal registry, no `z-modal-{n}` scale.
- [ ] **X2 — redirect the old Pages project** to `app.terav.fit`. Cloudflare
      dashboard action, not verifiable from the repo.
- [ ] **X3 — iOS PWA splash images.** `public/` has `apple-touch-icon.png` only;
      no `startup-image` link in `layout.tsx`.
- [ ] **FOUNDER — V5: `--color-slate` does three jobs** (info, CTA, neutral).
      Splitting it is a design call.
