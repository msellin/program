# Handover — 2026-09-04

Supersedes `HANDOVER-2026-09-03.md`. **Read this one; that one is wrong in
three places** — its open items A, B and C are all closed below.

Everything pushed and CI-green through `5dfaf53`. **732 tests, 61 files**
(was 674/57). Persona sweep re-run 2026-09-04: 23 runs, 246 behavioural
checks, 0 failures — `dev/audits/app/2026-09-04-persona-sweep.md`.
Working tree clean.

---

## THE ONE THING TO INTERNALISE

Yesterday's handover said the recurring defect is a correct decision
applied to one file and not its siblings. Still true — three more
instances today. But the sharper lesson from this session is different:

**The repo's stale CLAIMS are as dangerous as its stale code, because they
are written in the voice of something verified.**

Every item below was resolved by *checking* an assertion that had been
sitting in a comment, a test note or a handover, sounding settled:

- item C said a training max was "seeding a number in the user's store".
  It is `null`, and nothing reads `starting_values_kg` to seed anything.
- item B said the rest timer "needs a product call". It needed a bug fix;
  three timers were counting `setInterval` ticks.
- `ResumeLastRoute`'s comment said restoring the route was enough. It is
  not, and the comment was written by the session that shipped it.
- the SR-3 objection was called unresolved for two handovers. Twenty
  minutes of grepping resolved it.

**Before acting on any claim in this repo, check it.** Four of six commits
today came from doing that, and in every case the answer differed from
what the note said.

**And the founder's steer, which was right:** I twice labelled a
determined call "needs the founder" (the rest timer, then the expired-rest
UI) and once claimed a decision was mine when it was not (terminology).
Default: check first, decide what is genuinely determined, bring him only
what turns on his judgement.

---

## WHAT LANDED (6 commits)

1. **`e3bc8dc` — failed attempts.** Sets carry `failed`; always `reps: 0`.
   `SetView` shows "Missed N kg", gated to loadable lifts WITH a training
   max — that gate is the founder's off-plan objection answered, and it
   must not be loosened. `tm-plausibility` reads it as a dated ceiling
   (only misses on or after the best made lift; lightest binds).
   Siblings fixed in the same commit: `report.ts` (a miss would have been
   the clinical report's top set), `lastSessionSetsFor`, the log list.
2. **`7acf061`** — backfilling the historical 122 is CLOSED, founder's
   call, do not reopen. It would change no output.
3. **`4940cee` — wall clock.** `lib/wall-clock.ts`; all three timers.
4. **`9f8a2d5` — session cursor.** `lib/session-cursor.ts`; `DaySession`
   and `OffPlanSession`.
5. **`410dbf6`** — item C closed, nothing to delete.
6. **`c349f61`** — submax-HR evidence audit, rest persistence,
   `starting_values_kg` pinned as decorative.

---

## OPEN — MINE, IF ANY

**One, and it is the best available coverage win:** `RestTakeover` sits at
**8/14 controls**, unchanged by the 2026-09-04 sweep and the weakest
surface in the fleet. Six controls no persona has ever driven.
`session-rest-extend` skips with "rest takeover did not open" on the skill
and mobility personas, and `session-cold-reload` only probes the takeover
when a rest was actually running.

Otherwise nothing. Do not invent work; pick from the list below or ask.

---

## OPEN — NEEDS THE FOUNDER

**1. Background the app mid-set on the real PWA.** Highest value on this
list and only he can do it. Three backgrounding fixes shipped today on
tests that model what iOS *should* do. The tests simulate a suspended web
view; they cannot prove Safari's eviction behaviour. A green suite is not
evidence the model is right.

**2. ~~`submax_hr_pace5_bpm`~~ — DECIDED 2026-09-04, `5dfaf53`.**
`true_non_response` is suppressed; `under_dosing` and the metric stay. The
red "accept genetic ceiling" card no longer fires anywhere.

The deciding argument was NOT the evidence, it was the asymmetry — wrong
`under_dosing` costs a slightly harder session, wrong `true_non_response`
costs someone an arc that was working. Worth remembering as a pattern: the
founder has no sports-science credential and said so rather than guessing,
and the right answer turned out not to need one.

Correction carried into the audit doc: "no evidence supports this metric"
was too strong. Submax HR at a FIXED workload is real; the precondition is
what is missing.

What is STILL open and still his, but now lower stakes because nothing
user-facing depends on it:
   - the primary signal uses `sorted[1]` while the secondary uses
     `last - first`, six lines apart in `buildMetricCtx`. One is wrong.
   - `trend_slope` / `window_days` are declared and unread — wire or
     delete.

Re-enabling `true_non_response` needs a MEASUREMENT, not a code change: a
real workload anchor (block-2's 20-min threshold test), the declared trend
actually implemented, and sign-off from someone qualified. **Removing a
discouraging claim needs no expertise; restoring one does.**

**3. Overhead-mobility terminology.** Splits three ways and only one third
is mechanical: published paper TITLES must never change (falsifying a
reference); the FADIR/hip hit is femoroacetabular impingement, still
current, and is also a YouTube search string; the real item is the causal
claim at `overhead-mobility.json:455` — renaming the word leaves a
contested claim standing under a newer label. Rewriting a shipped
programme's rationale is authoring clinical content.

**4. `starting_values_kg`** — wire it up or delete it. Changes what every
new user is seeded with. Pinned by a test until decided.

**5. Carried, untouched:** SR-panel §C prescriptions, the VERIFIED badge,
EVID-1 outreach, Resend key deletion, QA-1, S4, S3 billing.

**Blocked on data/infra:** F2 Phase B (30 days), F4, FLAG-5.

---

## HOUSE RULES (unchanged, plus two)

All of yesterday's still apply — `npm run verify` from `next-app`, never
`--no-verify`, regenerate reviewer packets after programme data, Zod
strips unknown keys, an unresolved `exercise_id` does not throw, match the
CI run to your SHA.

Two to add:

- **Mutation-test, and include the schema.** Nineteen mutations today,
  every one caught. The important one: most tests build stores with
  `as unknown as Store` and bypass Zod entirely, so a stripped key is
  invisible. `failed-attempt.test.ts` round-trips through `storeSchema`
  deliberately.
- **A component test finds what a unit test cannot.** `restoredExpired`
  was derived from the current clock and flipped true the moment a
  legitimately-running restored rest hit its target, suppressing the
  completion it existed to fire. Only the rendered test caught it.

## SUPABASE

Unchanged from yesterday, except: **nothing was written.** The founder
declined the 122 backfill and no write was made. Project
`wrrqzpqgggxhcyabmmlm` (eu-west-3). Ask before writing; the 2026-09-03
permission was for one specific entry and was not used. Note the auto-mode
classifier blocked a read this session — expect to ask.
