# Cut C code sprint — what remains

> **Triaged 2026-09-08.** Every `- [ ]` in the original was checked against the
> codebase, not against its own description. Result: **32 already done and
> never ticked · 4 obsolete or superseded · 1 genuinely remaining.**
>
> The full original is preserved at
> `dev/archive/triaged-2026-09-08/active__cut-c-code-sprint__tasks.md` — nothing was deleted, and the
> DONE verdicts are recoverable from there. What follows is only what survived,
> so the generated index in `dev/active/OPEN-TASKS.md` counts reality instead of
> counting boxes nobody walked back to tick.

## Remaining

- [ ] **Rewire 8 stale `/progress` links to `/record`.** Cosmetic — every one
      redirects correctly — but they are the last references to a route that is
      now a redirect stub: `report/page.tsx:173`, `report/error.tsx:60`,
      `check/hip/page.tsx:51`, `BeliefsSection.tsx:203`, `SignalsStrip.tsx:347`,
      `:363`, `:416`, `StatusCards.tsx:216`.

The sprint itself shipped: `--dv-*` tokens (`globals.css:70-76`), the whole
`/record` route and its seven `Cut*` components, the JSON export, the bottom-nav
entry, the tour capture and the `persona-strength-long` persona.
