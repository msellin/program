# Open tasks — as of 2026-09-07

**Index of every workstream, plus the decisions currently on the founder.**
It is NOT the whole backlog — see the generated table at the bottom, which is
where "what is the full task list?" now has one answer.

Its previous header claimed to supersede the handovers. It did not: a 56-item
video-analysis plan, a 270-item master list and twenty other files were outside
it, and on 2026-09-08 that led me to tell the founder a feature with a passed
GO/NO-GO was "not in the pipeline". The table is generated by
`dev/scripts/task-index.py` so it cannot drift back.

48 commits on 2026-09-05/08; tests 674 -> 835.

## Mine

| P | Task | Est | Status |
|---|---|---|---|
| P2 | Persona sweep #8 — prove the deferral check finally runs | 31 min | **open** — the fix is dry-run green against sweep #7's artifacts, which is not the same as having run. Three sweeps in a row reported this path verified-by-nothing; the first sweep printing "2/2 verified" is the first evidence it works |
| P1 | **Off-plan strength has nowhere to go** | 2 h | **open, new 2026-09-09** — the founder benched today; "bench press" is not among the 134 exercises and there is no path to log a lift outside the programme's blocks, so it had to be filed as `activity_type: "other"`. Terav's own positioning is "runs alongside your existing week" — and the part of that week that is barbell work is invisible to it |
| P1 | **External load never reaches the proposal path** | 2 h | **open, new 2026-09-09** — Monday carried a 5.79 km run and 106 min of HIIT alongside the squats. The engine saw a green day and an RPE 7 top set and proposed a TM bump, with no knowledge of either. `runLogSchema`'s own comment says a Z3 today should count as external load for tomorrow's top set; nothing implements that |
| P2 | **Video analysis — unblock it** | 1 h | **open, new 2026-09-08** — the feature's own `tasks.md` has 42 open / 2 blocked and its gate is `V0-5`, the phone benchmark: the one item that can *invalidate* the design rather than delay it. Phase 0 passed (HSPU 100% detection, core 0.912); nothing since 2026-09-01 |
| P4 | Traffic: content, structured data, Search Console | days | **open, blocked** on the VERIFIED badge and the evidence page settling — see `dev/active/traffic/` |

## Yours — decisions only I cannot make

| P | Task | Est | Status |
|---|---|---|---|
| P1 | **Background the app mid-set on Android** | 2 min | **open — the only P1 left, and I cannot do it.** Real-device proof for four eviction fixes |
| P2 | **Overhead-mobility Push targets** | 20 min | **open, narrowed** — Progression 180° → Push 185° is a 5° step inside the metric's own 9° MDC, so a Push user cannot demonstrate reaching anything Progression did not. Stretch 190° is >2 SD above the measured healthy supine mean (177°, SD 6). Recorded as an ACCEPTED exception in `measurement-error.test.ts`; moving a target is yours |
| P2 | Withdraw `kilding_2012` and its claim — do not re-source | 20 min | **open, new 2026-09-07** — the rowing literature argues against it. Possamai 2022: critical power 37% above MLSS in rowing, "much larger than in running and cycling"; Beneke 1995: 4 mmol and IAT "do not represent MLSS workload". The programme never measures lactate — it prescribes a 2K-pace offset. `kilbey_2025` (PMID 40019691) is the honest replacement for a *differently worded* claim |
| P2 | Build `SELF_REPORT_TO_TEST_VAR` for muscle-up + overhead-mobility | 30 min | **open** — without it a skipper's test vars are 0, `tier_b_transition` is unreachable, and everyone lands in the lowest tier. Needs your call on what "3-5 ring dips" maps to. Intake copy no longer promises a proxy in the meantime |
| P2 | Capability slots that yield nothing | 1 h | **open, and the reported symptom was the wrong one** — foundation never gets an empty block because `reference_week_foundation` never schedules loaded work at all (phases 2 and 3 are named for it). The block that DOES ship empty is `block_thoracic_prep` for **push** users, 4 days/week, all 3 phases. Three gaps pinned by a guard test |
| P2 | Pull-up Tier B eccentric volume | 20 min | **narrowed** — evidence settled (Spudic & Nosaka 2025: no advantage for a concentric goal), citation corrected. What remains is the entry-intensity ramp, which no study specifies |
| P2 | Muscle-up false-grip from Tier A | 20 min | **narrowed** — needs a coach, not a clinician. Walker 2023 suggests bar-before-rings |
| P3 | Log Friday's two activities | 3 min | **open — checked, not stale** — 2026-09-04 has the hip drills (dead bug 3×6, 90/90 hip switch 3×12) and no `runs[]`. You logged a hyrox on Sep 6, so the mechanism works; Friday was never backfilled |
| P3 | `starting_values_kg`: wire up or delete? | 20 min | **open** — pinned by a test. `Boolean(tms.starting_values_kg)` gates the whole TM-bump engine while none of its values are read; deleting it silently kills progression |
| P3 | Resend key — is a second one live? | 2 min | **still unchecked** — the permission classifier blocked the connector's key listing. Needs you, or a permission grant |
| P4 | SR-panel §C remainder, EVID-1, QA-1, S4, S3 billing | — | **open** — admin and prescription decisions |

## Found but not yet triaged (from the 2026-09-07 integrity audit)

| Item | Where | Class |
|---|---|---|
| muscle-up tier `starting_capability_levels`: 4 of 5 keys match no domain | `muscle-up.json` ↔ `plan-generator.ts:555` | DEAD — the 2026-09-06 rename moved block slots and not tier keys |
| CSM amber rule enforced in code by hardcoded slug, and its data key is dead | `plan-generator.ts:83-102`, `CSM.json:502` | LIVE(code)/DEAD(data); the "resume at 3×4" half is unimplemented, and it may not fire on the production read path at all |
| `shoulder_pain_stop_rule` consent recorded, never enforced or resurfaced | `muscle-up.json:382` | Same family as `shoulder_pain_stops_session` |
| `authoring_note` is stripped by Zod | `schemas.ts:508-531` | DEAD — author-facing only, invisible to tooling |
| `store.cycle` is wholly dead, not just `phase_id` | `schemas.ts` ↔ nothing | DEAD — found 2026-09-11 closing H2; `cycle_number` and `week_in_cycle` are written and never read either. Removing live-looking state structure is a product call, so it was not folded into the H2 delete |
| `blocks[].tm_calculation`, `attached_to`, `phase_gated_optional`, `protocol`, `cautions` | `anterior-hip-rebuild.json` | DEAD — nested, so the top-level dead-key guard never saw them. `phase_gated_optional` and `cautions` read as behaviour and safety copy. Baselined in `data-integrity.test.ts` as OPEN, not approved |
| handstand-walk tier keys `handstand_obstacles`, `hollow_body_shape`, `wrist_extension_mobility` | `handstand-walk.json` ↔ its own `capability_slot`s | ORPHAN, not drift — checked the file's whole history and none was ever a slot, so there is nothing to rename them TO. The programme's premise text names obstacles as a separate state variable and no block composes against it. Deleting them would erase the intent; baselined in `ORPHAN_TIER_DOMAINS` |
| `phases[].gates_on`, `phases[].retest_gate` | `handstand-walk.json` | DEAD — `phase_0_bail_out_prep` declares it applies to users answering `bail_out_readiness` never_inverted/would_fall/can_step_out. Nothing reads it, so the bail-out phase is gated on nothing. Same baseline |

## Blocked on data or infrastructure

| Task | Blocker |
|---|---|
| F2 Phase B | needs 30 days of data |
| F4 | EU object storage |
| FLAG-5 | analytics decision |

## Closed 2026-09-11

**The CSM amber safety rule does not reach a real user.** This was on the
register as a suspicion ("it may not fire on the production read path at all").
It is worse than the suspicion, and now established:

- `applyProgramSoftening` withdraws `block_4x4_row` after three amber days. It
  lives inside `blocksForDate`, behind `if (store)` — the sixth and optional
  parameter, so any caller that omits it gets the unsoftened plan with no
  error and no failing test.
- `TodaySession` has two paths. The legacy one passes the store. The
  `block_object` one reads materialized `scheduled_blocks` and **never calls
  `blocksForDate` at all**.
- `StoreHydrator:159` sets `block_object: true` whenever the flag is
  undefined. Its own comment: "the flag is effectively on for everyone unless
  they opted out on Profile."

So the softened path is the one almost nobody is on. `materialize-blocks.ts`
omits the store too, so there is currently nowhere in the block-object flow
where the rule could apply. Five callers skip it in total, and
`composeDailyPlan` turns out to have **no callers anywhere in src** — a wholly
dead module.

Not fixed here: whether the rule belongs at materialization or at read over
materialized blocks decides whether a withdrawn block disappears from a user's
saved week or only from today's view. That is a prescription decision.
`softening-reach.test.ts` pins every load-bearing fact, including the
default-on flag, so it cannot be rediscovered a third time.

**Baseline integrity record.** `personas.baseline-pre-batch-36/` holds 910
files and had no manifest — the existing `2026-08-20-batch-36-baseline-manifest.sha256`
names two paths under that tree out of 910, so it was never the record it
looked like. `dev/scripts/baseline-manifest.py` now writes and, by default,
VERIFIES it. Blind-walk scoring compares against exactly this tree.

**Lint: 165 problems to 52.** `public/sw.js` is generated, minified serwist
output — linting it reported a `no-this-alias` error at column 4286 of line 1,
a finding about a bundler's codegen. Now ignored. The `_`-prefix convention the
codebase already uses for deliberate placeholders was never configured, so
every intentional `_param` reported as a defect; configured. Dead imports
removed, including `layout.tsx` still importing `BottomNav`, `StoreHydrator`
and `RestTimerHost` after they moved to `AppShell`. Two unescaped apostrophes
fixed; the `rules-of-hooks` hit on `tests/e2e/fixtures.ts` is a false positive
(Playwright's `use` fixture, not React's) and is disabled with a reason.

Left: 20 `setState`-synchronously-in-effect and 3 React Compiler render-purity
errors. Those are behaviour, not tidying.

### Checked and NOT what the register said

- **`block_thoracic_prep` (T16)** — reproduces exactly, and is ALREADY pinned:
  `slot-inventory.test.ts` carries all three gaps in `KNOWN_GAPS`, guarded both
  directions. Cause is the `±1` level window in `composeSlotDrills` against a
  slot-only block with no authored items to fall back on. Whether to author
  harder thoracic drills or widen the window is prescription.
- **`ProposalStack` → `ui/ProposalCard` (T54)** — the premise is wrong. The
  SHIPPING card already renders `CitationRef` when a proposal carries a
  `citationId`; the "affordance users never see" does exist. What remains is
  two divergent implementations, which is design consolidation, not a
  correctness fix.
- **`starting_values_kg` (T25)** — already pinned, and its own test says the
  remaining question changes what every new user is seeded with. Founder's.


**muscle-up's tier capability levels were pointing at renamed slots.** 8fde4c2
(2026-09-01) renamed four BLOCK slots to match first-strict-pullup's
convention and left the TIER keys on the old names. Nothing caught it because
the tier keys had no reader at all until 2026-09-06 — so the drift was
invisible for five days and then silently disabled `multi_dimensional`, the
premise these programmes are sold on, for four of muscle-up's five domains.
Levels fell through a Proxy to the flat tier baseline and every capability
moved together again.

The mapping was taken verbatim from that commit rather than inferred. Verified
against history: at authoring on 2026-08-18 the slots and tier keys matched
exactly, all five.

`capability-levels.test.ts` had a case on `mu_transition_control` and **passed
the whole time the feature was broken** — it asked `deriveLevelsFromProfile`
for the domain using the TIER's key, which existed, while the real consumer
asked for the BLOCK's slot, which did not. The test and the app were reading
different keys. Now uses the slot name.

handstand-walk's three are a different animal and are NOT fixed: checked the
file's entire history and `handstand_obstacles`, `hollow_body_shape` and
`wrist_extension_mobility` were never `capability_slot`s, so there is no
rename target. They are orphans — the programme declares a starting level for
a capability no block composes against, including obstacles, which its own
premise text calls a separate state variable. That needs authoring, not a
rename, so they are baselined rather than deleted.

**Off-plan could not reach a lift your programme does not prescribe.** The
"lifts you did elsewhere" group was added 2026-09-03 and only half-solved it:
it still filtered `program.blocks`, so a group whose entire premise is "this
was not on the plan" was sourced from the plan. A front squat worked because
the founder's programme has a front-squat block; a bench press did not,
because no programme has a bench block and `bench_press` was not among the 134
exercises at all. The session was filed as `activity_type: "other"` on
`runLogSchema` — an aerobic shape with distance, minutes and heart rate, and
nowhere to put sets, reps or weight. The lift was recorded as cardio.

Six barbell lifts authored into the shared library (bench, strict overhead
press, barbell row, weighted pull-up, loadable dip, RDL) — the library grew
out of a hip-rehab corpus and had no horizontal or vertical press of any kind.
The group now draws from the library minus whatever the programme already
contributes, under a synthetic `off_plan_library` block id; logs are keyed
`${blockId}:${exerciseId}` as a flat string, so nothing migrates.

Verified in the running app rather than argued: signed in, seeded a programme
with no bench block, and logged a set. It landed as
`off_plan_library:bench_press` with `sets: [{weight_kg, reps, rpe, failed}]`.

Surfacing the whole library exposed a collision the narrow list had hidden —
`farmer_carry` and `pu_farmers_carry` are both "Farmer's carry", so two
adjacent rows read identically and both said "3 sets". `variant` already
existed for this (it was split out of `name` so cards could render it); lists
just never used it. One `exerciseListName` helper now, used by the off-plan
list and both rails.


**Dependencies: 8 vulnerabilities → 0, both apps.** `next` 16.3.0 → 16.3.4
(critical RCE advisories; low real exposure on a static export with no Node
server, but a same-major patch), `sharp` → 0.35.4, and `nanoid` / `fast-uri` /
`browserslist` pinned via `overrides` — all three are build-time toolchain
deps that never reach the browser bundle. npm's own suggested fix for
`@serwist/next` was a semver-major DOWNGRADE to 9.4.1, which would have moved
the PWA service worker backwards to silence a transitive `browserslist`
advisory; overriding `browserslist` forward fixes it without touching serwist.
Both apps rebuilt clean.

**`authoring_note` declared instead of deleted — and the guard that missed it
widened.** The key was nested inside `intake.safety_gates[]`, and the dead-key
test reads `Object.keys(raw)`: top level only, despite a docstring promising
that a program "can declare anything and it simply vanishes". Parsing every
shipped program and diffing against source found **19 distinct nested paths
being discarded**, and they were not all prose:

- `blocks[].items[].reps_per_set` — handstand-walk's bail-out drills authored
  `sets: 5, reps_per_set: 2`. The field is `reps`. **Four falling-safety drills
  were rendering as five sets of nothing.** Fixed; intent was unambiguous.
- `phases[].gates_on` / `retest_gate`, `blocks[].phase_gated_optional`,
  `blocks[].cautions` — gates and safety copy, all silently dropped. Logged for
  the author rather than blessed or deleted.

The guard now walks to any depth, with two separate maps: documented-only
(nothing is supposed to read this) and OPEN_DROPPED_KEYS (something plainly
was). Collapsing those would turn a defect into a convention. Mutation-tested
both ways — a new drop fails, and a FIXED one fails until delisted.

**The withdrawn 3x-bodyweight figure was still shipping.** `sadowski_2021` was
corrected on 2026-09-05: no Sadowski paper exists, the DOI always resolved to
Mizutori et al., it is a floor study not parallel bars, and the 3x figure lives
in that paper's introduction quoting a third party. All three `used_for` fields
were updated to say so. **The copy was never swept** — four prose strings in
handstand-walk and muscle-up went on telling the user "~3x BW shoulder moment"
for six days, so a programme's evidence block said "do not cite this" while the
next screen cited it. Replaced with the paper's real finding, which supports
the same rule better: less-skilled performers produced LARGER peak shoulder
flexion moments. Pinned by a test that exempts `used_for` and the other record
fields, since a withdrawal has to be allowed to name what it withdrew.


**CI was red for a reason that had nothing to do with staleness.** Three runs
failed on `task-index: STALE` since the guard landed on 2026-09-08, and the
message was wrong every time it printed. `actions/checkout` defaults to depth
1, so the runner holds a single parentless commit and `git log -1 -- <path>`
returns it for EVERY tracked file: all sixteen workstreams come back stamped
with HEAD's date. The index CI generated could therefore never match the one
anyone committed, no matter how recently it was regenerated — and it was never
reproducible locally, because a normal clone has the history.

Verified by cloning this repo at `--depth 1` and diffing the dates:
saas-launch reads 2026-09-11 shallow against 2026-08-18 full, video-analysis
2026-09-11 against 2026-09-01, traffic 2026-09-11 against 2026-09-06.

Fixed with `fetch-depth: 0` rather than by loosening the test, because the
dates are the point — the script's own docstring says a workstream with 81 open
items and no commits for three weeks is telling you something a count alone
hides, and under a shallow clone every one of those dates reads as today. The
shallow case now prints its own diagnostic instead of sending the reader to
regenerate a correct file, and `fetch-depth: 0` is pinned by a test bounded to
the verify job. Both were mutation-tested; the bounded-slice version caught a
mutation that moved the setting to a later job, which the first draft did not.


**H2 — `store.cycle.phase_id` deleted.** Written in six places, read in none;
the wire option had died with `coach-client.ts`. Removed from `storeSchema` and
from all 23 call sites. The delete rests on Zod stripping unknown keys, so every
stored store and all 37 persona artifacts still parse — that was a code comment
and is now `store-schema-compat.test.ts`, because this repo has twice shipped
keys that Zod silently discarded and the silence is what let them survive.

**CLAUDE.md's program count.** It said "5 shipping programs" while the manifest
held nine, and had been wrong since `first-strict-pullup`, `muscle-up` and
`engine-builder-block-2` shipped. Wrong in the direction that matters: a session
briefed on five reasons about five, and this repo's recurring defect class —
a hip program's symptom map rendered to everyone, a load axis hardcoded to two
programs' lifts — is exactly what you miss when you think the catalog is
smaller than it is. Now pinned to the manifest in `data-integrity.test.ts`.
Mutation-tested, and the first version FAILED that test: it searched all of
CLAUDE.md, so deleting `muscle-up` from the catalog list still passed because
the Validation section mentions muscle-up for an unrelated reason. Scoped to
the paragraph.

## Closed 2026-09-05/07

**Sweeps and harness:** sweep #6 · sweep #7 (24 passed, checks 263→319, controls
98%, 0 never-driven) · the 6-check drop explained (stale reports, not a
regression) · `SWEEP_ID` stamping · `OffPlanSheet` + `NoteSheet` coverage ·
`offplan-drill-library` flow · flow→surface map completed and guarded ·
deferral check date fragility.

**Engine:** failed-attempt logging · wall-clock timers ×3 · session cursor ·
rest restore · Android `resume` listener · `true_non_response` suppressed ·
intake deferrals reaching production · "Adjusted for you" scoped to the day ·
`buildMetricCtx` delta · tier base level · taper phase schema · negative
numbers in tier conditions · per-capability independence · starting phase ·
HERITAGE compliance forwarding · 50 invisible rules.

**Evidence:** 44 overstatements · 7 wrong-paper URLs · 3 contradicting
citations · duplicate ids merged · 5 phantoms labelled · `ferrari_2021`
withdrawn · `billat_2001` corrected (2001→2003, PMID 12744715) · 6 programme
references that linked to search boxes · self-report proxy promise made honest.

**Infra:** sitemap + robots · 14 agent definitions tracked.

**The four P1 decisions, 2026-09-07:** VERIFIED badge derived from citation
verification rather than asserted in the manifest (all 8 demoted to CITED,
which is true) · 26 safety gates that were blocking by omission now declare
`severity: "block"`, `ring_dip_count` included and its warn/block question
resolved in the safe direction · measurement error respected — MDC forwarded
end-to-end, within-error deltas muted instead of green, tier gap at 160-165
closed, `muir_2010` adopted for the phantom `kim_2013` · escalation surfaced —
`redStreak` counts what `evaluateOverperformer` never had a counterpart for,
and BriefView shows the programme's own sentence at the moment it applies.

## Every workstream

<!-- TASK-INDEX:START -->

**16 workstreams · 171 open · 4 in progress · 2 blocked · 431 done.**

Generated by `dev/scripts/task-index.py` — do not hand-edit between the
markers. Counts are read from each file; the date is its last commit, so a
workstream with many open items and an old date is stalled, not busy.

| Workstream | Open | In progress | Blocked | Done | Last touched |
|---|---:|---:|---:|---:|---|
| [saas-launch](dev/active/saas-launch/tasks.md) | 60 | 0 | 0 | 48 | 2026-08-18 |
| [video-analysis](dev/active/video-analysis/tasks.md) ⚠ | 42 | 1 | 2 | 11 | 2026-09-01 |
| [saas-launch](dev/active/saas-launch/plan.md) | 36 | 0 | 0 | 0 | 2026-08-15 |
| [app](dev/audits/app/2026-08-19-open-task-list.md) | 9 | 0 | 0 | 0 | 2026-09-08 |
| [app](dev/audits/app/2026-08-19-master-task-list.md) | 6 | 2 | 0 | 252 | 2026-09-08 |
| [post-audit-p0s](dev/active/post-audit-p0s/implementation-plan.md) | 7 | 0 | 0 | 0 | 2026-09-08 |
| [batch-36](dev/active/batch-36/tasks.md) | 6 | 0 | 0 | 0 | 2026-09-08 |
| [traffic](dev/active/traffic/traffic-tasks.md) | 4 | 0 | 0 | 4 | 2026-09-06 |
| [failed-attempt](dev/active/failed-attempt/failed-attempt-tasks.md) | 0 | 1 | 0 | 16 | 2026-09-08 |
| [sr-panel-c](dev/active/sr-panel-c/sr-panel-c-tasks.md) | 1 | 0 | 0 | 10 | 2026-09-05 |
| [background-resume](dev/active/background-resume/background-resume-tasks.md) | 0 | 0 | 0 | 17 | 2026-09-08 |
| [harness-coverage](dev/active/harness-coverage/tasks.md) | 0 | 0 | 0 | 20 | 2026-09-08 |
| [offplan-flag](dev/active/offplan-flag/tasks.md) | 0 | 0 | 0 | 12 | 2026-08-24 |
| [optional-items](dev/active/optional-items/tasks.md) | 0 | 0 | 0 | 8 | 2026-09-03 |
| [personal-cue-separation](dev/active/personal-cue-separation/tasks.md) | 0 | 0 | 0 | 11 | 2026-09-01 |
| [program-audits-2026-09](dev/active/program-audits-2026-09/tasks.md) | 0 | 0 | 0 | 22 | 2026-09-02 |

<!-- TASK-INDEX:END -->
