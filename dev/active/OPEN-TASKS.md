# Open tasks — as of 2026-09-07

**Index of every workstream, plus the decisions currently on the founder.**
It is NOT the whole backlog — see the generated table at the bottom, which is
where "what is the full task list?" now has one answer.

Its previous header claimed to supersede the handovers. It did not: a 56-item
video-analysis plan, a 270-item master list and twenty other files were outside
it, and on 2026-09-08 that led me to tell the founder a feature with a passed
GO/NO-GO was "not in the pipeline". The table is generated by
`dev/scripts/task-index.py` so it cannot drift back.

48 commits on 2026-09-05/08; tests 674 -> 835.

## Mine

| P | Task | Est | Status |
|---|---|---|---|
| P2 | Persona sweep #8 — prove the deferral check finally runs | 31 min | **open** — the fix is dry-run green against sweep #7's artifacts, which is not the same as having run. Three sweeps in a row reported this path verified-by-nothing; the first sweep printing "2/2 verified" is the first evidence it works |
| P1 | **Off-plan strength has nowhere to go** | 2 h | **open, new 2026-09-09** — the founder benched today; "bench press" is not among the 134 exercises and there is no path to log a lift outside the programme's blocks, so it had to be filed as `activity_type: "other"`. Terav's own positioning is "runs alongside your existing week" — and the part of that week that is barbell work is invisible to it |
| P1 | **External load never reaches the proposal path** | 2 h | **open, new 2026-09-09** — Monday carried a 5.79 km run and 106 min of HIIT alongside the squats. The engine saw a green day and an RPE 7 top set and proposed a TM bump, with no knowledge of either. `runLogSchema`'s own comment says a Z3 today should count as external load for tomorrow's top set; nothing implements that |
| P2 | **Video analysis — unblock it** | 1 h | **open, new 2026-09-08** — the feature's own `tasks.md` has 42 open / 2 blocked and its gate is `V0-5`, the phone benchmark: the one item that can *invalidate* the design rather than delay it. Phase 0 passed (HSPU 100% detection, core 0.912); nothing since 2026-09-01 |
| P4 | Traffic: content, structured data, Search Console | days | **open, blocked** on the VERIFIED badge and the evidence page settling — see `dev/active/traffic/` |

## Yours — decisions only I cannot make

| P | Task | Est | Status |
|---|---|---|---|
| P1 | **Background the app mid-set on Android** | 2 min | **open — the only P1 left, and I cannot do it.** Real-device proof for four eviction fixes |
| P2 | **Overhead-mobility Push targets** | 20 min | **open, narrowed** — Progression 180° → Push 185° is a 5° step inside the metric's own 9° MDC, so a Push user cannot demonstrate reaching anything Progression did not. Stretch 190° is >2 SD above the measured healthy supine mean (177°, SD 6). Recorded as an ACCEPTED exception in `measurement-error.test.ts`; moving a target is yours |
| P2 | Withdraw `kilding_2012` and its claim — do not re-source | 20 min | **open, new 2026-09-07** — the rowing literature argues against it. Possamai 2022: critical power 37% above MLSS in rowing, "much larger than in running and cycling"; Beneke 1995: 4 mmol and IAT "do not represent MLSS workload". The programme never measures lactate — it prescribes a 2K-pace offset. `kilbey_2025` (PMID 40019691) is the honest replacement for a *differently worded* claim |
| P2 | Build `SELF_REPORT_TO_TEST_VAR` for muscle-up + overhead-mobility | 30 min | **open** — without it a skipper's test vars are 0, `tier_b_transition` is unreachable, and everyone lands in the lowest tier. Needs your call on what "3-5 ring dips" maps to. Intake copy no longer promises a proxy in the meantime |
| P2 | Capability slots that yield nothing | 1 h | **open, and the reported symptom was the wrong one** — foundation never gets an empty block because `reference_week_foundation` never schedules loaded work at all (phases 2 and 3 are named for it). The block that DOES ship empty is `block_thoracic_prep` for **push** users, 4 days/week, all 3 phases. Three gaps pinned by a guard test |
| P2 | Pull-up Tier B eccentric volume | 20 min | **narrowed** — evidence settled (Spudic & Nosaka 2025: no advantage for a concentric goal), citation corrected. What remains is the entry-intensity ramp, which no study specifies |
| P2 | Muscle-up false-grip from Tier A | 20 min | **narrowed** — needs a coach, not a clinician. Walker 2023 suggests bar-before-rings |
| P3 | Log Friday's two activities | 3 min | **open — checked, not stale** — 2026-09-04 has the hip drills (dead bug 3×6, 90/90 hip switch 3×12) and no `runs[]`. You logged a hyrox on Sep 6, so the mechanism works; Friday was never backfilled |
| P3 | `starting_values_kg`: wire up or delete? | 20 min | **open** — pinned by a test. `Boolean(tms.starting_values_kg)` gates the whole TM-bump engine while none of its values are read; deleting it silently kills progression |
| P3 | Resend key — is a second one live? | 2 min | **still unchecked** — the permission classifier blocked the connector's key listing. Needs you, or a permission grant |
| P4 | SR-panel §C remainder, EVID-1, QA-1, S4, S3 billing | — | **open** — admin and prescription decisions |

## Found but not yet triaged (from the 2026-09-07 integrity audit)

| Item | Where | Class |
|---|---|---|
| muscle-up tier `starting_capability_levels`: 4 of 5 keys match no domain | `muscle-up.json` ↔ `plan-generator.ts:555` | DEAD — the 2026-09-06 rename moved block slots and not tier keys |
| CSM amber rule enforced in code by hardcoded slug, and its data key is dead | `plan-generator.ts:83-102`, `CSM.json:502` | LIVE(code)/DEAD(data); the "resume at 3×4" half is unimplemented, and it may not fire on the production read path at all |
| `shoulder_pain_stop_rule` consent recorded, never enforced or resurfaced | `muscle-up.json:382` | Same family as `shoulder_pain_stops_session` |
| `authoring_note` is stripped by Zod | `schemas.ts:508-531` | DEAD — author-facing only, invisible to tooling |
| **13 display sites still convert dates through UTC** | `utc-date-keys.test.ts` baseline | **LIVE BUG CLASS** — `lib/utils.ts` exports `iso()` and documents exactly why `toISOString()` is wrong east of UTC; the pattern kept being written anyway. Two traced to concrete wrong behaviour and fixed 2026-09-11; 19 remain, several prescription-critical (`materialize-blocks`, `IntakeClient` start date) |
| Mid-session pain has no input | four programmes' `shoulder_pain_stop_rule` | The consent is about pain appearing DURING a set. The morning check cannot reach that moment, and there is no in-session symptom input. Resurfacing the rule is done; detecting it is a design change |
| `store.cycle` is wholly dead, not just `phase_id` | `schemas.ts` ↔ nothing | DEAD — found 2026-09-11 closing H2; `cycle_number` and `week_in_cycle` are written and never read either. Removing live-looking state structure is a product call, so it was not folded into the H2 delete |
| `blocks[].phase_gated_optional`, `protocol`, `cautions` — all on ONE block | `anterior-hip-rebuild.json` | **`block_smolov_jr_squat` is scheduled by nothing.** A fully authored 4-week optional peaking block — prescription, gating and four safety cautions — and its id appears once in the file, in its own definition. Making it reachable is a prescription decision. `tm_calculation` and `attached_to` resolved 2026-09-11 | OLD — nested, so the top-level dead-key guard never saw them. `phase_gated_optional` and `cautions` read as behaviour and safety copy. Baselined in `data-integrity.test.ts` as OPEN, not approved |
| handstand-walk tier keys `handstand_obstacles`, `hollow_body_shape`, `wrist_extension_mobility` | `handstand-walk.json` ↔ its own `capability_slot`s | ORPHAN, not drift — checked the file's whole history and none was ever a slot, so there is nothing to rename them TO. The programme's premise text names obstacles as a separate state variable and no block composes against it. Deleting them would erase the intent; baselined in `ORPHAN_TIER_DOMAINS` |
| `phases[].retest_gate` | `handstand-walk.json` | Declares how a user EXITS phase 0 — bail_out_confidence >= 6 for two days, coach nudge past two weeks. Phases are date-driven and phase 0 is one authored week, so none of it happens and no such log field exists. `gates_on` was deleted 2026-09-11: it duplicated a LIVE `phase_gates[]` entry | OLD — `phase_0_bail_out_prep` declares it applies to users answering `bail_out_readiness` never_inverted/would_fall/can_step_out. Nothing reads it, so the bail-out phase is gated on nothing. Same baseline |

## Blocked on data or infrastructure

| Task | Blocker |
|---|---|
| F2 Phase B | needs 30 days of data |
| F4 | EU object storage |
| FLAG-5 | analytics decision |

## Closed 2026-09-11

**The UTC date-key sweep — and the worst one was on the write path.**

`materialize-blocks.datesInRange` started its walk at LOCAL midnight and
converted each step back through UTC. Local in, UTC out, inside one function.
In UTC+3, `datesInRange("2026-09-11", "2026-09-13")` returned
`["2026-09-10", "2026-09-11", "2026-09-12"]`.

**Every materialized block was keyed a day early** — on the block-object WRITE
path, which `StoreHydrator` enables for everyone who has not opted out. And
`blockInstanceId` embeds `plannedDate`, so the instance ids were wrong with
them, which matters because those ids are what Skip and Move address.

Eleven sites fixed in total, each traced to a concrete wrong behaviour rather
than swept on a regex:

- `materialize-blocks` — the above, plus `materializeLookahead`'s window end
- `ensure-materialized.addDays` and `legacy-to-blocks`' ±28-day migration
  window — the boundaries that FEED the materializer, same bug one call earlier
- `IntakeClient` — `phase_shift_days` computed by subtracting a UTC date from a
  locally-parsed one. That number decides which phase a user is in
- `IntakeClient` / `ProgramPreviewClient` — the deterministic seed, which must
  agree between the two or a user seeded on preview and on intake gets two
  different plans
- `postgres-adapter.todayISO()` — returned YESTERDAY for anyone logging between
  local midnight and the offset. Exactly the people logging a late session
- `StoreHydrator` ×2, and an intake date input whose `min` accepted yesterday

**Two existing tests failed, and that is the finding.** Both computed their
EXPECTED value with the same `.toISOString().slice(0, 10)` the implementation
used, so expectation and defect shifted together and the suite stayed green
across a real bug. Replaced with hand-written literals — and with an assertion
on the input date too, since a literal that silently depends on programme JSON
is a literal only until someone edits the JSON.

`lib/proposals/select.ts` is now listed as explicitly NOT a bug: it anchors on
`T12:00:00Z`, and noon UTC is the same calendar day in UTC±11. That is the
correct defensive idiom, listed so nobody "fixes" it into the defect.

Thirteen sites remain, all rendering a date rather than keying on one. Listed
rather than exempted, because the distinction is a judgement each time.


**Two baselined dead-key clusters, read one key at a time — and they were four
different things, not two.**

`tm_calculation` was **user-facing instruction**: "TM = 5RM × 0.90. Set values
in the app's Training Maxes tab immediately after this session." Authored on
both evaluation blocks, discarded at parse, never shown to the person whose job
it described. Folded into `note`, which renders.

`attached_to` is vestigial — `block_runs` declares `attached_to:
"run_sessions"` while what actually schedules it is `category: "run"` plus
`phase_gated`. Moved to documented-only.

`phase_gated_optional`, `protocol` and `cautions` are ONE finding, not three:
all sit on `block_smolov_jr_squat`. It is a fully authored optional peaking
block — a 4-week, 4-day-per-week protocol the founder has personal history
with, plus four safety cautions including "first amber week, drop out
immediately" and "requires TM known to be accurate, do not run on estimated
TM". **Its id appears exactly once in the file, in its own definition.** Unlike
every sibling it is gated by `phase_gated_optional` rather than `phase_gated`,
so the discarded key is its only route in. Scheduled by nothing. Reaching it is
a prescription decision — its own note says it substitutes for cycle 4 weeks
1-3 and displaces pull frequency.

**`gates_on` was deleted rather than implemented, and the audit had it
backwards.** It was recorded as an unenforced gate leaving the bail-out phase
ungated. It is not: `phase_gates[]` declares the same rule — same phase, same
question, same answers — and `isPhaseGateSkipped` reads it. Driving
`activePhaseFor` across all four tiers and both answers shows the gate working:
`never_inverted` gets the falling-safety week, `can_exit_reliably` skips it.

So the defect was a DUPLICATE, which is a drift hazard in the direction nobody
checks — an author correcting the dead copy changes nothing and believes they
have. Deleted; `phase-zero-gate.test.ts` carries the record, asserts the
surviving gate behaves for every tier, and asserts the intake question feeding
it still offers those answers.

`retest_gate` stays open and is genuinely unimplemented: it describes exiting
phase 0 on `bail_out_confidence >= 6` for two consecutive days, with a coach
nudge if fear persists past two weeks. No such log field exists.


**Beta users can now say something.** There was no channel at all. The app asks
for a symptom score every morning and an Accept or Ignore on every proposal,
and offered nowhere to report that a proposal made no sense — the only route
was knowing the founder personally, which describes this beta and not the next
one.

`POST /api/feedback` + `/feedback` + a Profile row, because an installed PWA
has no address bar. Built against the three ways a mail form goes wrong, each
with a test:

- **Open relay** — an unauthenticated endpoint that mails a fixed address is a
  spam vector with a nice UI. The JWT is verified against Supabase before
  anything sends.
- **Spoofable sender** — the address comes from the VERIFIED TOKEN, never the
  request body, so nobody can make feedback appear to come from another user.
  `reply_to` is set to them, so answering is one tap.
- **Leaking the log** — the endpoint never reads the user's store. Context
  (programme slug, path) is attached client-side. A feedback form is not a
  reason to give a mail function access to Article 9 health data, and the test
  asserts the words `logs`, `symptoms` and `training_maxes` appear in neither
  the function nor the page.

It also says so explicitly when `RESEND_API_KEY` is missing rather than failing
quietly — that binding is a runtime Pages value CI cannot see, and the silent
version is a user typing out a considered bug report and watching it vanish.

**The new lint gate caught my own error on its first outing** — an unescaped
apostrophe in the feedback page — and the route-title suite caught a missing
`layout.tsx`. Both exactly what they exist for.


**Lint: 27 errors to 0, and gated for the first time.**

It ran nowhere automatic. `npm run verify` is vitest, the pre-commit hook runs
verify, `npm run deploy` runs verify — eslint ran when somebody chose to.

Twenty of the 27 were one React Compiler rule, `set-state-in-effect`, firing on
an idiom this codebase uses correctly. Five were read in full before touching
the config: `useUserPrefs` is the canonical SSR-hydration-safe localStorage
read (moving it into the initialiser CAUSES the mismatch it looks like it
avoids); `StickyCta` derives an offset from VirtualKeyboard / visualViewport,
which do not exist until mount; `YourPlanCard` counts views into localStorage;
`TodaySession` and `plan/page` call `setPrograms([])` in the empty-guard branch
of an async loader keyed on a value that only changes on a real change.
Downgraded to a warning with that reasoning in the config.

That mattered because of the other seven:

- a `use` disable directive that **silenced nothing** — a five-line
  explanation sat between `eslint-disable-next-line` and the code, so the
  directive applied to a comment. Two placements failed before one worked,
  both silently.
- `Date.now()` during render in `StatusCards`, which also **bypassed the
  `?today=` override** the app provides for testing at fixed dates. The one
  number on the card that silently refused to be tested.
- a `useMemo` whose deps named `program?.schedule_constraints` and
  `program?.goals` where the body depends on `program` — so a new object with
  equal sub-objects would not re-run it. That memo is the SAFETY GATE deciding
  whether someone may start a programme. Fixed rather than disabled; the
  compiler was right.

One file-scoped disable remains, with its reason: `IntakeClient` writes
`stepAnnounceRef.current` during render so the announce effect reads the step
being rendered NOW. Deriving it in the effect would announce the PREVIOUS step
to a screen reader, which is worse than the lint.

`npm run lint` now runs in CI's verify job, pinned by `lint-gate.test.ts` —
which also asserts the lint script still calls eslint, because a gate pointed
at `echo ok` reports green.


**Persona capability seeds (H1) — and the count was wrong twice.** Reported
first as "0 of 23 personas define capabilitySeed", later as "7 of 9 programmes
unseeded". Both overcount: anterior-hip-rebuild, concurrent-strength-maintenance
and rowing-2k-test-prep declare NO physical tests, so there is nothing to seed
and never was. **Six are seedable; two were seeded; all six now are.**

Every value is derived from the programme's own tier-A condition rather than
invented, because personas set no `tier` and `resolveActiveTier` falls back to
`plan_tiers[0]`. A muscle-up seed of 5 pull-ups and 4 ring dips satisfies
`tier_b_transition` — the fleet would silently exercise a different tier from
the one its own report claims. The engine programmes are the exception and the
test says so: they tier on intake answers, so their seeds cannot move the tier
and exist only so retest cards render a real baseline instead of "— · — · —".

17 assertions, mutation-tested three ways: dropping a programme's seed, typing
a seed key wrong, and a seed that quietly moves the tier all fail.

**QA-1 — the machine-checkable half.** A batch claimed eleven shipped fixes and
at least three were only partially executed; the plan was archived as complete
while the JSON said otherwise. `verify-shipped-claims.py` checks the one thing
free to check: a line marked `- [x]` naming a source file should name a file
that exists.

It paid for itself immediately. `saas-launch/tasks.md` ticks
`functions/api/state.ts`, `middleware.ts` and `src/lib/sync.ts` as DONE and
none exists — they belong to the Clerk-and-KV design that Supabase and Postgres
replaced. **Three done items describing an architecture this project does not
have.** Baselined with that reason rather than quietly deleted.

Mutation-tested both directions: a new unresolvable claim fails, and a
baselined claim that starts resolving fails until delisted.

The POLICY half — whether a batch may be archived without a proof line — is a
founder call and is not what the check asserts.


**Two accepts recorded no evidence.** `acceptDayAdjustment` has snapshotted its
citation since the feature shipped. Its two siblings on the same `switch` —
`promoteTier` and `advancePhase` — did not, so the two most consequential
accepts in the app (promoting a tier, moving a user bodily forward through
their programme) were written with no trace of why. `tier_history[]` recorded a
promotion and nothing else. The repo's signature defect in its purest form: the
right thing built, one sibling missed.

Snapshot rather than id, matching the sibling — `citations.json` is edited, and
papers here have been withdrawn and re-attributed, so resolving an id later can
show a different sentence from the one the user agreed to. The shape was
inlined twice already and is now `citationSnapshotSchema`.

Deliberately did NOT add a `log_signal` column. The mechanic is "cites a study
OR names its log signal" so it looks obviously right, but no proposal payload
carries one — it would be written by nothing, which is exactly
`daily_log_schema` and `progression_rules.states[]` again. The field arrives
with its writer or not at all.

**Re-run intake, from Profile.** Intake answers drive safety gates, tier
placement and drill composition, and were answerable exactly once — at the
moment a user knew least about the programme. `restartProgram` deliberately
PRESERVES them, so restarting was no way back in either. The route and the
overwrite already existed; what was missing was a door, and an installed PWA
has no address bar to type one into.

**`blind-walk.py` has now actually been run** — written for the Batch 36 gate,
completed, never executed, while batches 37, 38, 38.1 and 39 shipped on top of
an unevaluated one. Running it produced the finding: **840 slots**, which is
three and a half hours of scoring at fifteen seconds each. A gate nobody can
finish is not a gate. Added `--sample`, which draws PAIRS rather than screens
(sampling entries would hand the scorer the baseline of one screen and the post
of another) from the same seeded shuffle, so runs stay reproducible.

A real 40-pair packet is committed at `dev/audits/app/blind-walk-2026-09-11/`.
It compares baseline against CURRENT rather than against Batch 36 —
`personas.post-batch-36/` was never generated and the fleet has been
regenerated many times since, so the Batch-36-only comparison no longer exists
and would be the less useful one anyway. Also worth knowing: **2211 screens
exist only in the post set**, so the app has grown by more surfaces than the
baseline contains and the gate says nothing about those.

### Not done, and why

**`undoLastProposalOutcome` (T47) cannot be built from what is recorded.**
`proposal_history` stores `{id, kind, outcome, at, date, citation_snapshot}`
and `training_max_provenance` keeps source and timestamp but **not the previous
value** — so an accepted TM bump has no "before" to restore, and a tier
promotion's prior tier is only in `tier_history`. The prerequisite is capturing
reversal state at outcome time. Building that capture without the undo would
add a field nothing reads, and a partial undo that works for some proposal
kinds is worse than none. Reclassified with its real dependency rather than
half-built; the undo toast (T48) follows it.

**V0-7 and V5-4 are not open work.** V0-7 verifies model variants cache in the
service worker — there are no models. V5-4 writes privacy copy for a feature
that does not ship, which would be a claim about behaviour that does not exist.
Both belong behind the V0-5 gate.


**The amber rule now runs for the users who actually exist — and it was
counting the wrong week anyway.**

Applying it on the `block_object` path turned out to be parity, not a
decision: the legacy path filters at read, so this one does too. A withdrawn
block disappears from today's view and the saved week is untouched;
`scheduled[]` is filtered to match, or Skip/Move would keep offering verbs for
a block the safety rule just removed.

Then the test failed on its first run, and the reason was a second bug
underneath the first. The amber window walked dates with
`toISOString().slice(0, 10)`. **In UTC+3 — where this app is developed and
used — `back = 0` resolved to YESTERDAY.** The window covered days -1 to -7
instead of 0 to -6, so today's amber day was never counted: the single most
relevant day to a rule about whether to train today. A user on their third
consecutive amber day was read as having had two, and kept the block. Even on
the legacy path, where the rule did run, it had been reading the wrong week.

`lib/utils.ts` exports `iso()` and its docstring names this exact failure —
"in any timezone east of UTC that flips the calendar date at local midnight,
causing off-by-one bugs everywhere date strings are used as keys". The helper
existed; the pattern kept being written.

**21 files do it.** Two were traced to concrete wrong behaviour and fixed:
`plan-generator` (above) and `schedule.ts`'s `shiftIsoDate`, whose only callers
shift a programme's PHASE `starts` and `ends` by the user's intake
`phase_shift_days` — so every phase transition, and therefore which blocks a
user is prescribed and which day their retest falls on, was a day early. The
other 19 are baselined, not fixed: several are display-only where a slipped day
is cosmetic, and several are not — `materialize-blocks` keys the whole
block-object write path, and `IntakeClient` computes the programme start date.
Each needs reading.

**T21 is not a task, it is a decision already made.** Making the amber rule
data-driven means making `progression_rules.states[]` live, and CLAUDE.md rules
that out deliberately: a programme declares what feeds the gate, not how
lenient the gate is. Nine unreviewed threshold sets on the decision that tells
someone not to train. The threshold stays in code. What could reasonably move
into data is WHICH block is withdrawn — that is programme content, not
leniency — and that is worth a separate, smaller decision.

**Consented stop-rules are resurfaced.** Four programmes make the user tick "I
agree to stop the session if shoulder pain appears during a hang, transition,
or support hold." Required, so nobody starts without agreeing. It was then
never mentioned again — intake consents are written once and read nowhere.
Same shape as the escalation rule and `shoulder_pain_stops_session` before it,
fixed the same way: added to the `RulesAccordion` that already exists, leading,
because it is the one the user personally agreed to and the one that ends a
session. NOT enforcement — all four declare a `shoulder` symptom region so the
morning gate can see it, but the consent is about mid-session pain, which no
input reaches. Logged separately.

**`daily-plan.ts` deleted.** 127 lines, `composeDailyPlan`, zero callers
anywhere in src. It was carrying a permanent exception in the softening
baseline for code no user could reach.


**External load now reaches the proposal path.** The founder's Monday carried a
5.79 km run and 106 minutes of HIIT alongside the squats. The day read green,
the top set came in at RPE 7, and the engine proposed a training-max bump —
then explained itself with "3 straight green days. The engine reads that as
headroom."

Every input was already recorded and already audited. `daySignals` computes
`fatigue: "high"` at >=90 cardio minutes, and `runLogSchema`'s comment says a
Z3 today counts as external load for the next session. Neither was read:
`externalLoad` reached the weekly-narrative TILE and stopped, so its one
consumer was a display.

The correction is narrow on purpose. The green-streak check is untouched — it
is symptom-derived and means something else. Days at merely ELEVATED load still
count, because a concurrent athlete does cardio by design and disqualifying
that means they never earn a bump at all. Only `high` is withheld, and only as
EVIDENCE. The proposal also now names the outside training it did see, because
confirm-first only means something if what the user confirms is stated.

Test took three passes to become real. The first asserted `|| true`. The
second set `lifted` on all three days, so days 1-2 kept supplying evidence and
reverting the fix did not break it. The heavy day is now the sole carrier of
the evidence, and the mutation fails as it should.

**Account deletion was already built — the gap is a binding nobody wrote
down.** `/account` has the UI, `useAccountActions.deleteAccount` sends the JWT,
`DELETE /api/delete-account` verifies it and calls the Supabase admin API, and
migration `20260818000001` really does declare `ON DELETE CASCADE` on both
`user_states` and `user_state_snapshots`. Probed live: a bogus token returns
"Invalid session" rather than "Auth not configured", so the endpoint is
deployed and its auth bindings are set. The register entry predates the KV to
Postgres move.

What is NOT verifiable from outside is `SUPABASE_SERVICE_ROLE_KEY`, and it was
in no documentation anywhere. **There are two kinds of secret in this project
and only one was written down**: `NEXT_PUBLIC_*` is inlined at build time and
CI greps the artifact for it, while everything under `functions/` reads `env.*`
at REQUEST time from Pages project bindings — invisible to the artifact grep,
so all three deploy gates pass green with every one of them missing. Five such
bindings exist and none was documented.

Without the service-role key, account deletion — a GDPR obligation on Article 9
health data — returns 500 and does not happen. It fails SAFE (the check runs
after auth and before anything destructive, so nobody is half-deleted) but it
fails silently, and cannot be exercised end-to-end without a real session.

CLAUDE.md now separates build-time from runtime, and `runtime-env.test.ts`
asserts the documented list still matches what the functions read, plus that
the config check keeps preceding the destructive call.

**Founder check, and the only thing left on T09:** confirm
`SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`,
`ADMIN_EMAILS` and `RESEND_API_KEY` are set on the `app.terav.fit` Pages
project. Nothing in the repo can see them.


**CONTENT-1, and two of its five items were already fixed.**

- `engine-builder`'s Block 3 promise WAS removed from user-facing copy on
  2026-09-03. What survived was `goals.arc.block_3_polarise`, describing the
  block exactly like its two shipping siblings, five lines below a
  `status_note` saying it does not exist. Nothing renders `goals.arc`, so no
  user saw it — but an author reading the arc and trusting it is precisely how
  a removed promise returns. The rule was recorded in prose ("do not
  reintroduce the promise before the programme does"), which is a rule with no
  enforcement; it is a test now.
- Writing that guard surfaced a second, live gap: `engine-builder`'s
  `block_2_volume` carried **no status marker at all**, while
  `engine-builder-block-2` has been in the manifest for weeks. A user
  finishing Block 1 could not tell from the arc that the next block exists.
  And the two files disagreed about the same absent Block 3 — one said
  "expected", the other "NOT SHIPPED".
- **`rowing-2k` test-day pacing already ships.** `RacePlanCard` computes the
  four 500m splits from the user's own baseline and renders in `BriefView:218`.
  Its own docstring quotes the complaint. Stale register entry.
- The taper's `goal` said "Volume down 40-50%, intensity held" while the same
  phase's `block_replacements_final_week` swaps race-pace for easy recovery.
  The physiology is sound and documented; the one-line goal simply did not say
  it. Qualified.
- `submax_hr_pace5_bpm`'s display name was already made honest on 2026-08-21.
  Its NOTE was stale: it said RunLog has no pace field, and
  `avg_pace_500m_seconds` exists and is already used by rowing-2k. The real
  reason the metric has no anchor is that engine-builder is modality-agnostic
  and a 500m split anchors only the ergs. Corrected, so the next person does
  not chase a field that is already there.

**`capability_profile`: the obvious fix would undo T12.** The reported defect
is real — intake writes keys by PHYSICAL TEST ID, the reader expects a
capability DOMAIN, and the namespaces never intersect. But the writer sets
`estimated_level: tierBaseline` for EVERY test, so the map carries one number
repeated, not per-capability information. It is read AFTER the tier's authored
levels and overrides them.

So mapping the keys — the fix anyone would reach for — would flatten
muscle-up's `mu_transition_mechanics: 1` against its baseline of 2 back to a
single level, reintroducing exactly the defect T12 repaired, through the code
path added to fix it, while looking like progress. The real work is deriving a
level from `measured_value` against the programme's tier conditions: the same
authoring gap as `SELF_REPORT_TO_TEST_VAR`.

`capability-profile-trap.test.ts` demonstrates the regression rather than
asserting it, and fails if either half changes.


**The CSM amber safety rule does not reach a real user.** This was on the
register as a suspicion ("it may not fire on the production read path at all").
It is worse than the suspicion, and now established:

- `applyProgramSoftening` withdraws `block_4x4_row` after three amber days. It
  lives inside `blocksForDate`, behind `if (store)` — the sixth and optional
  parameter, so any caller that omits it gets the unsoftened plan with no
  error and no failing test.
- `TodaySession` has two paths. The legacy one passes the store. The
  `block_object` one reads materialized `scheduled_blocks` and **never calls
  `blocksForDate` at all**.
- `StoreHydrator:159` sets `block_object: true` whenever the flag is
  undefined. Its own comment: "the flag is effectively on for everyone unless
  they opted out on Profile."

So the softened path is the one almost nobody is on. `materialize-blocks.ts`
omits the store too, so there is currently nowhere in the block-object flow
where the rule could apply. Five callers skip it in total, and
`composeDailyPlan` turns out to have **no callers anywhere in src** — a wholly
dead module.

Not fixed here: whether the rule belongs at materialization or at read over
materialized blocks decides whether a withdrawn block disappears from a user's
saved week or only from today's view. That is a prescription decision.
`softening-reach.test.ts` pins every load-bearing fact, including the
default-on flag, so it cannot be rediscovered a third time.

**Baseline integrity record.** `personas.baseline-pre-batch-36/` holds 910
files and had no manifest — the existing `2026-08-20-batch-36-baseline-manifest.sha256`
names two paths under that tree out of 910, so it was never the record it
looked like. `dev/scripts/baseline-manifest.py` now writes and, by default,
VERIFIES it. Blind-walk scoring compares against exactly this tree.

**Lint: 165 problems to 52.** `public/sw.js` is generated, minified serwist
output — linting it reported a `no-this-alias` error at column 4286 of line 1,
a finding about a bundler's codegen. Now ignored. The `_`-prefix convention the
codebase already uses for deliberate placeholders was never configured, so
every intentional `_param` reported as a defect; configured. Dead imports
removed, including `layout.tsx` still importing `BottomNav`, `StoreHydrator`
and `RestTimerHost` after they moved to `AppShell`. Two unescaped apostrophes
fixed; the `rules-of-hooks` hit on `tests/e2e/fixtures.ts` is a false positive
(Playwright's `use` fixture, not React's) and is disabled with a reason.

Left: 20 `setState`-synchronously-in-effect and 3 React Compiler render-purity
errors. Those are behaviour, not tidying.

### Checked and NOT what the register said

- **`block_thoracic_prep` (T16)** — reproduces exactly, and is ALREADY pinned:
  `slot-inventory.test.ts` carries all three gaps in `KNOWN_GAPS`, guarded both
  directions. Cause is the `±1` level window in `composeSlotDrills` against a
  slot-only block with no authored items to fall back on. Whether to author
  harder thoracic drills or widen the window is prescription.
- **`ProposalStack` → `ui/ProposalCard` (T54)** — the premise is wrong. The
  SHIPPING card already renders `CitationRef` when a proposal carries a
  `citationId`; the "affordance users never see" does exist. What remains is
  two divergent implementations, which is design consolidation, not a
  correctness fix.
- **`starting_values_kg` (T25)** — already pinned, and its own test says the
  remaining question changes what every new user is seeded with. Founder's.


**muscle-up's tier capability levels were pointing at renamed slots.** 8fde4c2
(2026-09-01) renamed four BLOCK slots to match first-strict-pullup's
convention and left the TIER keys on the old names. Nothing caught it because
the tier keys had no reader at all until 2026-09-06 — so the drift was
invisible for five days and then silently disabled `multi_dimensional`, the
premise these programmes are sold on, for four of muscle-up's five domains.
Levels fell through a Proxy to the flat tier baseline and every capability
moved together again.

The mapping was taken verbatim from that commit rather than inferred. Verified
against history: at authoring on 2026-08-18 the slots and tier keys matched
exactly, all five.

`capability-levels.test.ts` had a case on `mu_transition_control` and **passed
the whole time the feature was broken** — it asked `deriveLevelsFromProfile`
for the domain using the TIER's key, which existed, while the real consumer
asked for the BLOCK's slot, which did not. The test and the app were reading
different keys. Now uses the slot name.

handstand-walk's three are a different animal and are NOT fixed: checked the
file's entire history and `handstand_obstacles`, `hollow_body_shape` and
`wrist_extension_mobility` were never `capability_slot`s, so there is no
rename target. They are orphans — the programme declares a starting level for
a capability no block composes against, including obstacles, which its own
premise text calls a separate state variable. That needs authoring, not a
rename, so they are baselined rather than deleted.

**Off-plan could not reach a lift your programme does not prescribe.** The
"lifts you did elsewhere" group was added 2026-09-03 and only half-solved it:
it still filtered `program.blocks`, so a group whose entire premise is "this
was not on the plan" was sourced from the plan. A front squat worked because
the founder's programme has a front-squat block; a bench press did not,
because no programme has a bench block and `bench_press` was not among the 134
exercises at all. The session was filed as `activity_type: "other"` on
`runLogSchema` — an aerobic shape with distance, minutes and heart rate, and
nowhere to put sets, reps or weight. The lift was recorded as cardio.

Six barbell lifts authored into the shared library (bench, strict overhead
press, barbell row, weighted pull-up, loadable dip, RDL) — the library grew
out of a hip-rehab corpus and had no horizontal or vertical press of any kind.
The group now draws from the library minus whatever the programme already
contributes, under a synthetic `off_plan_library` block id; logs are keyed
`${blockId}:${exerciseId}` as a flat string, so nothing migrates.

Verified in the running app rather than argued: signed in, seeded a programme
with no bench block, and logged a set. It landed as
`off_plan_library:bench_press` with `sets: [{weight_kg, reps, rpe, failed}]`.

Surfacing the whole library exposed a collision the narrow list had hidden —
`farmer_carry` and `pu_farmers_carry` are both "Farmer's carry", so two
adjacent rows read identically and both said "3 sets". `variant` already
existed for this (it was split out of `name` so cards could render it); lists
just never used it. One `exerciseListName` helper now, used by the off-plan
list and both rails.


**Dependencies: 8 vulnerabilities → 0, both apps.** `next` 16.3.0 → 16.3.4
(critical RCE advisories; low real exposure on a static export with no Node
server, but a same-major patch), `sharp` → 0.35.4, and `nanoid` / `fast-uri` /
`browserslist` pinned via `overrides` — all three are build-time toolchain
deps that never reach the browser bundle. npm's own suggested fix for
`@serwist/next` was a semver-major DOWNGRADE to 9.4.1, which would have moved
the PWA service worker backwards to silence a transitive `browserslist`
advisory; overriding `browserslist` forward fixes it without touching serwist.
Both apps rebuilt clean.

**`authoring_note` declared instead of deleted — and the guard that missed it
widened.** The key was nested inside `intake.safety_gates[]`, and the dead-key
test reads `Object.keys(raw)`: top level only, despite a docstring promising
that a program "can declare anything and it simply vanishes". Parsing every
shipped program and diffing against source found **19 distinct nested paths
being discarded**, and they were not all prose:

- `blocks[].items[].reps_per_set` — handstand-walk's bail-out drills authored
  `sets: 5, reps_per_set: 2`. The field is `reps`. **Four falling-safety drills
  were rendering as five sets of nothing.** Fixed; intent was unambiguous.
- `phases[].gates_on` / `retest_gate`, `blocks[].phase_gated_optional`,
  `blocks[].cautions` — gates and safety copy, all silently dropped. Logged for
  the author rather than blessed or deleted.

The guard now walks to any depth, with two separate maps: documented-only
(nothing is supposed to read this) and OPEN_DROPPED_KEYS (something plainly
was). Collapsing those would turn a defect into a convention. Mutation-tested
both ways — a new drop fails, and a FIXED one fails until delisted.

**The withdrawn 3x-bodyweight figure was still shipping.** `sadowski_2021` was
corrected on 2026-09-05: no Sadowski paper exists, the DOI always resolved to
Mizutori et al., it is a floor study not parallel bars, and the 3x figure lives
in that paper's introduction quoting a third party. All three `used_for` fields
were updated to say so. **The copy was never swept** — four prose strings in
handstand-walk and muscle-up went on telling the user "~3x BW shoulder moment"
for six days, so a programme's evidence block said "do not cite this" while the
next screen cited it. Replaced with the paper's real finding, which supports
the same rule better: less-skilled performers produced LARGER peak shoulder
flexion moments. Pinned by a test that exempts `used_for` and the other record
fields, since a withdrawal has to be allowed to name what it withdrew.


**CI was red for a reason that had nothing to do with staleness.** Three runs
failed on `task-index: STALE` since the guard landed on 2026-09-08, and the
message was wrong every time it printed. `actions/checkout` defaults to depth
1, so the runner holds a single parentless commit and `git log -1 -- <path>`
returns it for EVERY tracked file: all sixteen workstreams come back stamped
with HEAD's date. The index CI generated could therefore never match the one
anyone committed, no matter how recently it was regenerated — and it was never
reproducible locally, because a normal clone has the history.

Verified by cloning this repo at `--depth 1` and diffing the dates:
saas-launch reads 2026-09-11 shallow against 2026-08-18 full, video-analysis
2026-09-11 against 2026-09-01, traffic 2026-09-11 against 2026-09-06.

Fixed with `fetch-depth: 0` rather than by loosening the test, because the
dates are the point — the script's own docstring says a workstream with 81 open
items and no commits for three weeks is telling you something a count alone
hides, and under a shallow clone every one of those dates reads as today. The
shallow case now prints its own diagnostic instead of sending the reader to
regenerate a correct file, and `fetch-depth: 0` is pinned by a test bounded to
the verify job. Both were mutation-tested; the bounded-slice version caught a
mutation that moved the setting to a later job, which the first draft did not.


**H2 — `store.cycle.phase_id` deleted.** Written in six places, read in none;
the wire option had died with `coach-client.ts`. Removed from `storeSchema` and
from all 23 call sites. The delete rests on Zod stripping unknown keys, so every
stored store and all 37 persona artifacts still parse — that was a code comment
and is now `store-schema-compat.test.ts`, because this repo has twice shipped
keys that Zod silently discarded and the silence is what let them survive.

**CLAUDE.md's program count.** It said "5 shipping programs" while the manifest
held nine, and had been wrong since `first-strict-pullup`, `muscle-up` and
`engine-builder-block-2` shipped. Wrong in the direction that matters: a session
briefed on five reasons about five, and this repo's recurring defect class —
a hip program's symptom map rendered to everyone, a load axis hardcoded to two
programs' lifts — is exactly what you miss when you think the catalog is
smaller than it is. Now pinned to the manifest in `data-integrity.test.ts`.
Mutation-tested, and the first version FAILED that test: it searched all of
CLAUDE.md, so deleting `muscle-up` from the catalog list still passed because
the Validation section mentions muscle-up for an unrelated reason. Scoped to
the paragraph.

## Closed 2026-09-05/07

**Sweeps and harness:** sweep #6 · sweep #7 (24 passed, checks 263→319, controls
98%, 0 never-driven) · the 6-check drop explained (stale reports, not a
regression) · `SWEEP_ID` stamping · `OffPlanSheet` + `NoteSheet` coverage ·
`offplan-drill-library` flow · flow→surface map completed and guarded ·
deferral check date fragility.

**Engine:** failed-attempt logging · wall-clock timers ×3 · session cursor ·
rest restore · Android `resume` listener · `true_non_response` suppressed ·
intake deferrals reaching production · "Adjusted for you" scoped to the day ·
`buildMetricCtx` delta · tier base level · taper phase schema · negative
numbers in tier conditions · per-capability independence · starting phase ·
HERITAGE compliance forwarding · 50 invisible rules.

**Evidence:** 44 overstatements · 7 wrong-paper URLs · 3 contradicting
citations · duplicate ids merged · 5 phantoms labelled · `ferrari_2021`
withdrawn · `billat_2001` corrected (2001→2003, PMID 12744715) · 6 programme
references that linked to search boxes · self-report proxy promise made honest.

**Infra:** sitemap + robots · 14 agent definitions tracked.

**The four P1 decisions, 2026-09-07:** VERIFIED badge derived from citation
verification rather than asserted in the manifest (all 8 demoted to CITED,
which is true) · 26 safety gates that were blocking by omission now declare
`severity: "block"`, `ring_dip_count` included and its warn/block question
resolved in the safe direction · measurement error respected — MDC forwarded
end-to-end, within-error deltas muted instead of green, tier gap at 160-165
closed, `muir_2010` adopted for the phantom `kim_2013` · escalation surfaced —
`redStreak` counts what `evaluateOverperformer` never had a counterpart for,
and BriefView shows the programme's own sentence at the moment it applies.

## Every workstream

<!-- TASK-INDEX:START -->

**16 workstreams · 171 open · 4 in progress · 2 blocked · 431 done.**

Generated by `dev/scripts/task-index.py` — do not hand-edit between the
markers. Counts are read from each file; the date is its last commit, so a
workstream with many open items and an old date is stalled, not busy.

| Workstream | Open | In progress | Blocked | Done | Last touched |
|---|---:|---:|---:|---:|---|
| [saas-launch](dev/active/saas-launch/tasks.md) | 60 | 0 | 0 | 48 | 2026-08-18 |
| [video-analysis](dev/active/video-analysis/tasks.md) ⚠ | 42 | 1 | 2 | 11 | 2026-09-01 |
| [saas-launch](dev/active/saas-launch/plan.md) | 36 | 0 | 0 | 0 | 2026-08-15 |
| [app](dev/audits/app/2026-08-19-open-task-list.md) | 9 | 0 | 0 | 0 | 2026-09-08 |
| [app](dev/audits/app/2026-08-19-master-task-list.md) | 6 | 2 | 0 | 252 | 2026-09-08 |
| [post-audit-p0s](dev/active/post-audit-p0s/implementation-plan.md) | 7 | 0 | 0 | 0 | 2026-09-08 |
| [batch-36](dev/active/batch-36/tasks.md) | 6 | 0 | 0 | 0 | 2026-09-08 |
| [traffic](dev/active/traffic/traffic-tasks.md) | 4 | 0 | 0 | 4 | 2026-09-06 |
| [failed-attempt](dev/active/failed-attempt/failed-attempt-tasks.md) | 0 | 1 | 0 | 16 | 2026-09-08 |
| [sr-panel-c](dev/active/sr-panel-c/sr-panel-c-tasks.md) | 1 | 0 | 0 | 10 | 2026-09-05 |
| [background-resume](dev/active/background-resume/background-resume-tasks.md) | 0 | 0 | 0 | 17 | 2026-09-08 |
| [harness-coverage](dev/active/harness-coverage/tasks.md) | 0 | 0 | 0 | 20 | 2026-09-08 |
| [offplan-flag](dev/active/offplan-flag/tasks.md) | 0 | 0 | 0 | 12 | 2026-08-24 |
| [optional-items](dev/active/optional-items/tasks.md) | 0 | 0 | 0 | 8 | 2026-09-03 |
| [personal-cue-separation](dev/active/personal-cue-separation/tasks.md) | 0 | 0 | 0 | 11 | 2026-09-01 |
| [program-audits-2026-09](dev/active/program-audits-2026-09/tasks.md) | 0 | 0 | 0 | 22 | 2026-09-02 |

<!-- TASK-INDEX:END -->
